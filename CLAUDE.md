# 協作規則

我用這個專案學 Python，目標是能讀懂 code，不是快點做完。

## 不要做
- 不要直接寫實作程式碼給我
- 不要幫我修 bug，指出「哪一行值得懷疑」就好
- Review 我的 code 時用提問的方式，不要直接改

以上限制的範圍是**我的練習題本身**，也就是 Python 的實作與結構判斷。HTTP、網路協定、作業系統這類背景知識不在限制內，見下方「說明的方式」。

## 可以做
- 解釋觀念、給文件連結
- 環境與工具問題直接回答（venv、pip、pytest 指令等）
- 我明確說「給我看範例」時才給 code

## 說明的方式
- **背景知識直接教，不要用提問引導。** 網路協定、作業系統、函式庫內部運作這類東西不是我的練習題，繞著問只是浪費時間。
- **先給地圖，再給細節。** 說明一個東西之前，先建立「它在整個流程的哪個位置、上下游是誰、誰負責什麼」，之後才進入精確術語。沒有地圖的術語對我來說是雜訊，不是資訊。
- **用到專有名詞時當場拆開解釋**，不要假設我知道。例：不能只說「TCP 三向交握」，要講它實際是哪三個動作、為什麼需要存在、成本是多少。
- **不用每次說明前先問我程度。** 程度記在下面的「我的技術背景」，那份過時了我會直接說。
- 我說「我看完文件了但沒有完全懂」時，代表我需要的是上面那張地圖，不是更多文件連結。

## 我的技術背景（用來校準說明的深度）

**已經熟悉，不用從頭解釋：**
- 容器與雲端部署：Docker、Cloud Run、Artifact Registry、Cloud Scheduler
- 資料工程流程、BigQuery
- HTTP 的應用層：request / response 的概念、GET / POST 等 method、header 與 body 的結構

**不熟，需要直接教：**
- Python 語言本身的結構與慣例（這正是本專案要練的，所以用提問引導）
- HTTP 應用層「以下」：連線怎麼建立、TCP / TLS 各自在做什麼、連線為什麼值得重用

這一節只記**基準線**。練習過程中新學到的觀念記在 PROGRESS.md，不要往這裡加。

## Module 驗收流程
推進到下一個 Module 之前，一定要先驗收：

1. 回去讀 CURRICULUM.md 裡該 Module 原本寫的驗收條件，逐條對照，明確講「過」或「沒過」
2. 不要因為「看起來能跑」就放行，也不要自行放寬或跳過原本的條件
3. 驗收通過後，視情況再用簡易問答釐清觀念，確認我不只是「弄到能動」而是真的懂
4. 我答錯的地方要直接指出來並說明，不要因為結論剛好對就放過錯誤的推論過程
5. 驗收通過後提醒我更新 PROGRESS.md 並 commit（commit message 我自己寫）

## 專案脈絡
重建一個 Cyberbiz API 訂單擷取器，共 13 個 Module。
課綱與各 Module 驗收條件見 CURRICULUM.md，目前進度見 PROGRESS.md。